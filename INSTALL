1) type "make". If this fails because getoptlong is not found (e.g. on Solaris) then use the following command "CFLAGS=-DNO_GETOPT_LONG make -e"

2) then do whatever you want with the boincstat executable!
